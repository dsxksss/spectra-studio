export default {
    plugins: {
        // We leave this empty to prevent inheriting the parent directory's PostCSS config
        // which contains tailwindcss v3, conflicting with our v4 setup.
    },
}
